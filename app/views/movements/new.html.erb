<% content_for :title, "New Movement" %>

<section class="mx-auto max-w-3xl space-y-6">
  <div>
    <%= link_to "← Back to movements", movements_path, class: "text-sm font-bold text-zinc-400 transition hover:text-amber-200" %>

    <p class="mt-8 text-sm font-bold uppercase tracking-[0.22em] text-amber-200/80">Training Memory</p>
    <h1 class="mt-3 text-4xl font-black tracking-tight text-white">New Movement</h1>
    <p class="mt-4 max-w-2xl text-base leading-7 text-zinc-400">
      Add a lift or movement pattern you want Forge to remember. Current cues and progression rules will eventually support the Today view.
    </p>
  </div>

  <div class="rounded-3xl border border-white/10 bg-zinc-900/80 p-6 shadow-2xl shadow-black/30 sm:p-8">
    <%= render "form", movement: @movement %>
  </div>
</section>